# Copy to secrets.toml for local dev, or paste into the Streamlit Community Cloud
# app's Secrets panel. Never commit a real secrets.toml.

# The deployed Render backend, no trailing slash.
BACKEND_URL = "https://your-backend.onrender.com"
