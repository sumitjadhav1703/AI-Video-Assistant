# ── AI Video Assistant — FRONTEND (Streamlit Community Cloud) ─────────────────
# The frontend is a thin HTTP client. All processing, model calls, and vector
# storage live on the backend, so nothing else belongs here.

streamlit>=1.35.0
requests>=2.32.0
