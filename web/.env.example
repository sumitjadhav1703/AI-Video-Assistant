# Public URL of the FastAPI backend on Render (no trailing slash).
# Not a secret — it is a public HTTPS endpoint. Set the same value in Vercel.
VITE_BACKEND_URL=https://ai-video-assistant-backend.onrender.com
